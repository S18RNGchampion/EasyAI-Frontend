// const messageEncryptUtil = {
//   // Base64 编码
//   encrypt: (message) => {
//       return btoa(encodeURIComponent(message));
//   },
//   // Base64 解码
//   decrypt: (encrypted) => {
//       return decodeURIComponent(atob(encrypted));
//   }
// };

// export default messageEncryptUtil;
